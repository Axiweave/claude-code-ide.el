:PROPERTIES:
:CATEGORY: claude-code-ide.el
:END:
* DONE zmx integration
https://github.com/ArthurHeymans/emacs-term-sessions
or self build
* TODO zmx adopt-through-start renders stale until manual window resize
Adopting an existing zmx session via the start-time reattach offer shows a
squashed/stale ghostel buffer; a manual window resize fixes it.
Direct =claude-code-ide-attach= renders fine; both paths share
=claude-code-ide--create-session=.
Tried (both insufficient):
- PTY size jiggle via =set-process-window-size=
- =ghostel--adjust-size WINDOW t= on a 0.2s timer after display
Next suspect: timer fires before the zmx replay finishes; hook the forced
redraw to the first process output after attach instead. Also compare
window/grid sizes at display time between the two paths.
* TODO manager mouse hover highlight does not fill the whole row
The temporary highlight under the mouse pointer still does not span the full
manager row.
Tried (insufficient):
- =mouse-face (:inherit highlight :extend t)= on the row text without the newline
- zero-width manager window fringes through buffer-local values and
  =set-window-fringes=; the live window reported zero-width fringes, but the
  visual result did not change


* DONE global view with project + worktree
Implemented: [[file:specs/004-grouped-global-view/quickstart.md][Grouped global view acceptance results]].
Press =v= in the global manager to switch views.
#+begin_example
project-a
  1. main
  2. trunk
project-b
  3. main
  4. trunk-b

[ramhorn]
project-remote
  5. main
  6. trunk-b
#+end_example
C-j C-k to jump between project-group

* TODO git-less view
simplified git status without magit

* DONE tramp if needed
CLOSED: [2026-09-08 Tue 05:39]
close related buffers when detached

* DONE worktree creation
CLOSED: [2026-09-08 Tue]
worktrunk
https://github.com/lukeed/lane
Done in [[file:../../specs/002-magit-worktree-launch/quickstart.md][specs/002-magit-worktree-launch/quickstart.md]] of the configuration repository:
=claude-code-ide-manager-new-worktree= (=w=), =claude-code-ide-worktree-backend=,
=claude-code-ide-manager-open-directory=.

* DONE remote worktree acceptance
Manager =W= provides remote open, create, and retained results.
Remote Magit routes supported remove, move, merge, push, and prune actions
through the shared admission and receipt protocol.
Native move and push capture and isolated lifecycle checks pass.
T064/T075 record completed authorized real-host acceptance.
The accepted compatibility scope is Emacs 31/32. Emacs 28.1, 29.4,
30.1, and other native-compilation combinations remain unverified. See
[[file:../../specs/003-remote-worktree-management/tasks.md][specs/003-remote-worktree-management/tasks.md]].

* TODO checkout an existing branch into a new worktree
The new-worktree command refuses names that already exist as a branch.

* DONE remove eat and vterm; ghostel only forward
CLOSED: [2026-09-12 Sat]
Superseded by [[file:specs/008-remove-legacy-backends/plan.md][specs/008-remove-legacy-backends]] (Ghostel-only terminal cutover).

* DONE remote with-editor; omp integration
CLOSED: [2026-09-14 Mon]
Done in [[file:specs/010-attached-editor-handoff/plan.md][specs/010-attached-editor-handoff]]: the Agent asks the attached Emacs through an APC marker plus CSI-u ctrl+g, and the prompt opens in whichever Emacs is attached now. Needs an Agent started after the upgrade.
now it open editor in remote emacs! if the session is launch by remote emacs which wrapped its own with-editor.
find a way to open in my local emacs?
how? escape sequence? force to nvim (not ideal...)
how to make it transparent to match my local workflow?

assuming free to change omp

* TODO remote prompt open is slow on first use (~30 s)
While the remote-project worker waits on the RPC response, =accept-process-output= also runs Ghostel's event filter for the Session buffer. =ghostel--buffer-identification= calls =abbreviate-file-name= on the remote =default-directory= (=%d= spec), which makes a nested tramp-rpc call inside the outer wait. Slow, not a hang. Fix in Ghostel: skip =abbreviate-file-name= for remote paths, or drop =%d= from =ghostel-buffer-identification-format=.

* TODO harden magit-prime late sentinels
=leo/magit-prime-refresh-cache= now skips priming when =magit--refresh-cache= is nil. The upstream sentinel at =magit-prime.el:110-125= can still see a nil cache after the 1 s wait scope exits and leak one exited process per git call ("Too many open files"). Capture a stable cache in the sentinel and reap in =unwind-protect=, or send it upstream.
